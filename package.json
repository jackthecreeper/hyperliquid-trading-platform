{
  "name": "hyperliquid",
  "version": "1.0.0",
  "description": "A Hyperliquid trading platform with MetaMask integration and real-time prices from CoinMarketCap",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rsync -a --exclude='node_modules' --exclude='*.log' . dist/ && cp -r crypto-logo dist/"
  },
  "keywords": [
    "crypto",
    "trading",
    "metamask",
    "ethereum",
    "coinmarketcap"
  ],
  "author": "Hyperliquid",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.0.0",
    "nodemon": "^3.0.1"
  }
}
